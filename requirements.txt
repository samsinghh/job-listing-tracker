requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0
pytest>=8.0
# Optional: only needed if you implement Playwright-backed scrapers for the
# JS-rendered sites (Google, Meta, Jane Street).
# playwright>=1.44
